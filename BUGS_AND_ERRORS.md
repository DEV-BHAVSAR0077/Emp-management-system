# Bugs and Errors Report

| File Name | Error in Line | Error Explanation | Solution of Error | Solution Code | Where to Replace/Add |
|---|---:|---|---|---|---|
| `database/seeders/DatabaseSeeder.php` | 25 | The main seeder calls only `PermissionSeeder`. `DefaultRolesSeeder` exists but is not called, so fresh database seeding may create permissions without creating the default roles. | Call `DefaultRolesSeeder` before `PermissionSeeder`. | ```php\n$this->call([\n    DefaultRolesSeeder::class,\n    PermissionSeeder::class,\n]);\n``` | Replace the existing `$this->call([PermissionSeeder::class]);` line inside `run()`. |
| `database/seeders/DatabaseSeeder.php` | 25 | No seeder assigns permissions to the default roles. Authorization depends on role permissions through `Gate::before`, so seeded users can be unable to access user/role management features. | Add role-permission assignment after roles and permissions are seeded. | ```php\n$admin = \\App\\Models\\Role::where('name', 'Admin')->first();\n$hr = \\App\\Models\\Role::where('name', 'HR')->first();\n$user = \\App\\Models\\Role::where('name', 'User')->first();\n\n$admin?->permissions()->sync(\\App\\Models\\Permission::pluck('id'));\n$hr?->permissions()->sync(\n    \\App\\Models\\Permission::whereIn('name', [\n        'view-user', 'create-user', 'edit-user', 'delete-user',\n    ])->pluck('id')\n);\n$user?->permissions()->sync(\n    \\App\\Models\\Permission::whereIn('name', ['view-user'])->pluck('id')\n);\n``` | Add this inside `DatabaseSeeder::run()` after `$this->call([...]);`. |
| `database/seeders/DefaultRolesSeeder.php` | 14-16 | The `Admin` role is not seeded as protected even though the roles table has an `is_protected` column. | Add `is_protected => true` to `Admin` and `false` to other roles. | ```php\n['name' => 'Admin', 'description' => 'Full system access. Can manage users and roles.', 'color' => '#b45309', 'is_protected' => true],\n['name' => 'HR', 'description' => 'Can add, edit, and delete employee records.', 'color' => '#6d28d9', 'is_protected' => false],\n['name' => 'User', 'description' => 'Standard access. Can view users and edit own profile.', 'color' => '#374151', 'is_protected' => false],\n``` | Replace the three arrays inside `$defaults`. |
| `app/Providers/AppServiceProvider.php` | 24-25 | All authorization checks rely on `$user->hasPermission($ability)`. If a user's role does not exist or has no attached permissions, every `@can` and `Gate::authorize()` check fails. | Return `null` when permission is missing so Laravel continues normal authorization behavior, and avoid forcing false-like behavior. | ```php\nGate::before(function ($user, $ability) {\n    return $user->hasPermission($ability) ? true : null;\n});\n``` | Replace the current `Gate::before` callback inside `boot()`. |
| `tests/Feature/ExampleTest.php` | 6 | The feature test expects `/` to return HTTP `200`, but the route intentionally redirects guests to login. The actual response is HTTP `302`, so `php artisan test` fails. | Assert the expected redirect to login. | ```php\ntest('the application redirects guests to login', function () {\n    $response = $this->get('/');\n\n    $response->assertRedirect(route('login'));\n});\n``` | Replace the full test in `tests/Feature/ExampleTest.php`. |
| `routes/web.php` | 16 | This route redirects `/` to the login route. This is valid app behavior, but it conflicts with the default Laravel feature test. | No route code change is required if login redirect is intended. Fix the test instead. | ```php\nRoute::get('/', function () {\n    return redirect()->route('login');\n});\n``` | Keep this route as-is. |
| `package.json` | 6 | `npm run build` runs `vite build`, but `node_modules` is missing, so the command fails with `'vite' is not recognized`. | Install npm dependencies before building. | ```powershell\nnpm install\nnpm run build\n``` | Run these commands in `C:\\wamp64\\www\\dev`. No source replacement is needed. |
| `database/migrations/2026_04_22_030949_create_roles_table.php` | 20 | The `roles` table has an `is_protected` column, but the controller and views do not consistently use it. | Keep this column and use it in `RoleController` instead of hard-coded protected role names. | ```php\n$table->boolean('is_protected')->default(false);\n``` | Keep this migration line as-is. Update controller logic to use `$role->is_protected`. |
| `app/Http/Controllers/RoleController.php` | 16 | Protected roles are hard-coded as `['admin']`. This duplicates the `is_protected` database field and makes future protected roles harder to manage. | Remove the hard-coded constant and use the database column. | ```php\n// Remove this line:\nconst PROTECTED_ROLES = ['admin'];\n``` | Delete the `PROTECTED_ROLES` constant from the top of `RoleController`. |
| `app/Http/Controllers/RoleController.php` | 61-64 | Editing a protected role immediately redirects back to the dashboard, but the edit view says protected roles can update color and description. | Allow opening the edit page for protected roles. | ```php\npublic function edit(Role $role)\n{\n    Gate::authorize('edit-role');\n\n    $permissions = Permission::all();\n    $roles = Role::orderBy('name')->get();\n    $user = Auth::user();\n\n    return view('roles.edit_role', compact('role', 'permissions', 'roles', 'user'));\n}\n``` | Replace the whole `edit(Role $role)` method in `RoleController`. |
| `app/Http/Controllers/RoleController.php` | 78-82 | Updating a protected role is completely blocked, which conflicts with the edit view message that says color and description can be updated. | For protected roles, lock the name but allow description, color, and permissions updates. | ```php\npublic function update(Request $request, Role $role)\n{\n    Gate::authorize('edit-role');\n\n    $request->validate([\n        'name' => [\n            'required',\n            Rule::unique('roles', 'name')->ignore($role->id),\n        ],\n        'description' => 'nullable|max:255',\n        'color' => 'required',\n    ]);\n\n    $oldName = $role->name;\n    $newName = $role->is_protected ? $role->name : $request->name;\n\n    $role->update([\n        'name' => $newName,\n        'description' => $request->description,\n        'color' => $request->color,\n    ]);\n\n    if (! $role->is_protected && $oldName !== $newName) {\n        User::where('role', $oldName)->update(['role' => $newName]);\n    }\n\n    $role->permissions()->sync($request->permissions ?? []);\n\n    return redirect()->route('dashboard', ['tab' => 'roles'])\n        ->with('success', 'Role Updated Successfully');\n}\n``` | Replace the whole `update(Request $request, Role $role)` method in `RoleController`. |
| `app/Http/Controllers/RoleController.php` | 114-118 | Deleting a protected role is blocked using a hard-coded name check instead of the `is_protected` database field. | Check `$role->is_protected` instead of role name. | ```php\nif ($role->is_protected) {\n    return redirect()->route('dashboard', ['tab' => 'roles'])\n        ->with('error', 'The \"' . $role->name . '\" role is a system role and cannot be deleted.');\n}\n``` | Replace the `in_array(strtolower($role->name), self::PROTECTED_ROLES, true)` block inside `destroy()`. |

## Commands Used

| Command | Result |
|---|---|
| `php artisan route:list` | Passed. Routes loaded successfully. |
| `php artisan config:clear` | Passed. Configuration cache cleared. |
| `php artisan view:clear` | Passed. Compiled Blade views cleared. |
| `php artisan test` | Failed. `tests/Feature/ExampleTest.php` expected `200`, received `302`. |
| `npm run build` | Failed. `vite` was not found because `node_modules` is missing. |
| PHP syntax scan on project files | App files parsed cleanly before the scan timed out while continuing through `vendor`. |
